# 絕不入庫：登入憑證與任何 memory 資料 / 備份（本 repo 只放機制）
.claude.json
*.claude.json
projects/
memory/
*.bak.*
memory.bak.*

# notify-release：真實憑證與 per-project 設定絕不入庫（範本 *.example 仍可入庫）
notify-release.env
*.env
!*.env.example
notify-release.config.json

# 雜項
__pycache__/
*.pyc
sbx*/
.DS_Store
