# ~/.claude/notify-release.env — 共用 SendGrid 憑證（所有專案共用，一次設定）
# 真實檔案（含真正的 SENDGRID_API_KEY）絕不入庫。install.sh 只在此檔不存在時
# 用本範本建立一份 0600 的空殼，之後永不覆蓋；或改用 `/notify-release setup-env`。
SENDGRID_API_KEY="SG.your-sendgrid-api-key"
SENDGRID_FROM_EMAIL="you@example.com"
SENDGRID_FROM_NAME="Your Team"
